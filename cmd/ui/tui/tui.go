package tui

import (
	"fmt"

	"github.com/ebarthur/jotl/cmd/config"
	"github.com/ebarthur/jotl/cmd/logs"
)

func ShowDashboard(logStreamer *logs.LogStreamer, userConfig *config.JotlConfig) {
	fmt.Println("TUI Dashboard is currently under construction.")

	// log.Println(logStreamer)
	// log.Println(userConfig)

}
